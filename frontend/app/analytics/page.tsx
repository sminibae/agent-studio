import { SectionPlaceholder } from "@/components/section-placeholder";

export default function AnalyticsPage() {
  return (
    <SectionPlaceholder
      eyebrow="Analytics"
      title="Evidence for every iteration"
      description="Comparable scopes, coverage, regressions, and metrics will live here."
    />
  );
}
