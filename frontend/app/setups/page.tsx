import { SectionPlaceholder } from "@/components/section-placeholder";

export default function SetupsPage() {
  return (
    <SectionPlaceholder
      eyebrow="Setups"
      title="Agent and evaluation setups"
      description="Versioned assets and immutable setup snapshots will live here."
    />
  );
}
