import { SectionPlaceholder } from "@/components/section-placeholder";

export default function ExperimentsPage() {
  return (
    <SectionPlaceholder
      eyebrow="Experiments"
      title="Runs and traces"
      description="Execution batches, progress, failures, and trace details will live here."
    />
  );
}
